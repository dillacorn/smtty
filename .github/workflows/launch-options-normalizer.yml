name: Add profile section shortcuts

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch-and-validate:
    runs-on: ubuntu-latest

    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Apply section shortcut update
        run: python3 tools/add-profile-section-shortcuts.py

      - name: Preserve executable mode
        run: chmod +x smtty

      - name: Check Bash syntax
        run: |
          bash -n smtty
          bash -n tests/test-launch-options-normalizer.sh
          bash -n tests/test-compact-ui.sh
          bash -n tests/test-compact-prompts.sh
          bash -n tests/test-profile-workflow.sh
          bash -n tests/test-guided-quit.sh

      - name: Run test suites
        run: |
          bash tests/test-launch-options-normalizer.sh
          bash tests/test-compact-ui.sh
          bash tests/test-compact-prompts.sh
          bash tests/test-profile-workflow.sh
          bash tests/test-guided-quit.sh

      - name: Install ShellCheck
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck

      - name: Run ShellCheck
        run: shellcheck -S error smtty tests/test-launch-options-normalizer.sh tests/test-compact-ui.sh tests/test-compact-prompts.sh tests/test-profile-workflow.sh tests/test-guided-quit.sh

      - name: Check whitespace
        run: git diff --check

      - name: Commit section shortcut update
        run: |
          if git diff --quiet -- smtty tests/test-profile-workflow.sh; then
            echo "Profile section shortcuts already applied"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add smtty tests/test-profile-workflow.sh
          git commit -m "refine: add profile section shortcuts"
          git push origin HEAD:${{ github.head_ref }}
