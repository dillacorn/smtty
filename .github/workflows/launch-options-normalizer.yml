name: Refine launch-option normalizer

on:
  push:
    branches:
      - agent/normalize-launch-options
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  patch-and-validate:
    runs-on: ubuntu-latest

    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          fetch-depth: 0

      - name: Apply normalizer refinements
        run: python3 tools/refine-launch-options-normalizer.py

      - name: Check Bash syntax
        run: |
          bash -n smtty
          bash -n tests/test-launch-options-normalizer.sh

      - name: Run launch-option parser cases
        run: bash tests/test-launch-options-normalizer.sh

      - name: Install ShellCheck
        run: |
          sudo apt-get update
          sudo apt-get install -y shellcheck

      - name: Run ShellCheck error pass
        run: shellcheck -S error smtty tests/test-launch-options-normalizer.sh

      - name: Check whitespace errors
        run: git diff --check

      - name: Commit refined smtty
        run: |
          if git diff --quiet -- smtty; then
            echo "smtty is already refined"
            exit 0
          fi

          branch=${{ github.head_ref || github.ref_name }}
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add smtty
          git commit -m "fix: preserve advanced launch option semantics"
          git push origin HEAD:"$branch"
